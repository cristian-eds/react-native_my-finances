import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    padding: 8,
    backgroundColor: 'white',
    alignItems: 'center',
    borderRadius: 10
  },
  containerChart: {
    marginVertical: 8, 
    borderRadius: 16,
  }
});