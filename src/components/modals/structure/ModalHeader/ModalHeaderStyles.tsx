import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    header: {
        flexDirection: 'row',
        justifyContent: 'space-between',  
        alignItems: 'center',
        borderBottomWidth: 0.9,
        borderBottomColor: '#cacaca8a',
        paddingBottom: 10,
        marginBottom: 10,
    },
});