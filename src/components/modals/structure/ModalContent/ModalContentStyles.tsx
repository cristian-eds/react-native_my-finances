import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
      containerContent: {
        width: '98%',
        backgroundColor: 'white',
        padding: 20,
        borderRadius: 10,
        rowGap: 10,
    },
});