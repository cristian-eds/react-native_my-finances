import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    installmentItem: {
        borderBottomWidth: 1,
        borderBottomColor: '#d3d3d3',
        paddingBottom: 5,
    },
    title: {
        fontSize: 22,
        textAlign: 'center',
        marginBottom: 10,
        fontWeight: 'bold',
    },
});